import { useParams } from "react-router-dom";


const MakeOffer = () => {
    const {id} = useParams();
    return (
        <div>
            makeOffer
        </div>
    );
};

export default MakeOffer;